Checkbox for multi-select and option lists. Supports `indeterminate` via a ref on the native input.

```jsx
<Checkbox label="Include drafts" defaultChecked />
<Checkbox label="Sync media" description="Mirror images to the CDN on publish" />
```

Props: `label`, `description`, plus native checkbox attributes.
