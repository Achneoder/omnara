A square, label-only-icon button for toolbars and row actions — always pass `label` for accessibility.

```jsx
<IconButton label="Revert" variant="ghost"><i data-lucide="rotate-ccw" /></IconButton>
<IconButton label="Settings" variant="outline"><i data-lucide="settings" /></IconButton>
<IconButton label="New" variant="solid"><i data-lucide="plus" /></IconButton>
```

Variants: `ghost` (default), `outline`, `solid`. Sizes `sm | md | lg`.
